apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mailman.fullname" . }}
  labels:
    {{- include "mailman.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "mailman.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "mailman.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      hostname: {{ .Values.mailman.hostname }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "mailman.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: MM_HOSTNAME
              value: {{ .Values.mailman.hostname }}
            - name: MM_RUNNER_ARCHIVE_COUNT
              value: "{{ .Values.mailman.runners.archive.count }}"
            - name: MM_RUNNER_BOUNCES_COUNT
              value: "{{ .Values.mailman.runners.bounces.count }}"
            - name: MM_RUNNER_IN_COUNT
              value: "{{ .Values.mailman.runners.in.count }}"
            - name: MM_RUNNER_LMTP_COUNT
              value: "{{ .Values.mailman.runners.lmtp.count }}"
            - name: MM_RUNNER_OUT_COUNT
              value: "{{ .Values.mailman.runners.out.count }}"
            - name: MM_RUNNER_PIPELINE_COUNT
              value: "{{ .Values.mailman.runners.pipeline.count }}"
            - name: MM_RUNNER_REST_COUNT
              value: "{{ .Values.mailman.runners.rest.count }}"
            - name: MM_RUNNER_RETRY_COUNT
              value: "{{ .Values.mailman.runners.retry.count }}"
            - name: MM_RUNNER_TASK_COUNT
              value: "{{ .Values.mailman.runners.task.count }}"
            - name: MM_RUNNER_VIRGIN_COUNT
              value: "{{ .Values.mailman.runners.virgin.count }}"
            - name: MM_RUNNER_DIGEST_COUNT
              value: "{{ .Values.mailman.runners.digest.count }}"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.mailman.dbUrl.existingSecretName . }}
                  key: {{ tpl .Values.mailman.dbUrl.existingSecretKey . }}
            - name: MTA
              value: {{ .Values.mailman.mta }}
            - name: SMTP_HOST
              value: {{ .Values.mailman.smtpHost }}
            - name: SMTP_PORT
              value: "{{ .Values.mailman.smtpPort }}"
            - name: ARC_ENABLED
              value: "{{ .Values.mailman.arc.enabled }}"
            - name: ARC_DOMAIN
              value: "{{ .Values.mailman.arc.domain }}"
            - name: ARC_KEY_PATH
              value: "{{ .Values.mailman.arc.keyPath }}"
            - name: ARC_SELECTOR
              value: "{{ .Values.mailman.arc.selector }}"
            - name: ARC_AUTHSERVER_ID
              value: "{{ .Values.mailman.arc.authserver_id }}"
            - name: MAILARCHIVE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.mailman.mailarchiver.apiKey.existingSecretName . }}
                  key: {{ tpl .Values.mailman.mailarchiver.apiKey.existingSecretKey . }}
            - name: MAILARCHIVE_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.mailman.mailarchiver.baseUrl.existingSecretName . }}
                  key: {{ tpl .Values.mailman.mailarchiver.baseUrl.existingSecretKey . }}
            - name: MAILARCHIVE_DESTINATION
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.mailman.mailarchiver.destination.existingSecretName . }}
                  key: {{ tpl .Values.mailman.mailarchiver.destination.existingSecretKey . }}
          ports:
            - name: api
              containerPort: {{ .Values.service.apiPort }}
              protocol: TCP
            - name: lmtp
              containerPort: {{ .Values.service.lmtpPort }}
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /opt/mailman/var
      volumes:
        - name: data
        {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ default (include "mailman.fullname" .) .Values.persistence.existingClaim }}
        {{- else }}
          emptyDir: {}
        {{- end }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
